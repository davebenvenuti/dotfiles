#!/bin/sh

DOTFILES_PATH="~/.dotfiles"

if [ ! -e ~/.oh-my-zsh ]; then
  echo 'Installing/updating oh-my-zsh...'

  git clone git://github.com/robbyrussell/oh-my-zsh.git ~/.oh-my-zsh
fi

echo 'Symlinking custom zsh theme...'

ln -sf ~/.dotfiles/zsh/dbiddy.zsh-theme ~/.oh-my-zsh/custom/dbiddy.zsh-theme

echo 'Symlinking zshrc...'

ln -sf ~/.dotfiles/zsh/zshrc ~/.zshrc

echo 'Symlinking aliases...'

ln -sf ~/.dotfiles/aliases ~/.aliases

if [ ! -e ~/.rbenv ]; then
  echo 'Installing rbenv...'

  git clone git://github.com/sstephenson/rbenv.git ~/.rbenv
fi

echo 'Sourcing ~/.zshrc...'

source ~/.zshrc

if [ ! -e ~/.rbenv/plugins/ruby-build ]; then  
  echo 'Installing rbenv plugin ruby-build...'

  git clone git://github.com/sstephenson/ruby-build.git ~/.rbenv/plugins/ruby-build
fi

if [ ! -e ~/.emacs.d/el-get ]; then
  echo 'Installing el-get (emacs)...'

  git clone git://github.com/dimitri/el-get.git ~/.emacs.d/el-get
fi

echo 'Symlinking emacs.d stuff...'

ln -sf ~/.dotfiles/emacs/init.el ~/.emacs.d/init.el

echo 'Symlinking git config...'

ln -sf ~/.dotfiles/gitconfig ~/.gitconfig

if [[ `uname` =~ Linux ]]; then

  echo 'Installing xmonad and related apps/scripts...'

  sudo apt-get install xmonad xmobar stalonetray xautolock xscreensaver imwheel xfce4-power-manager cinnamon-settings-daemon feh

  git clone git@github.com:bchurchill/xmonad-pulsevolume.git ~/.xmonad-pulsevolume

  if [ ! -d ~/.xmonad ]; then
    echo 'Creating xmonad config folder...'

    mkdir ~/.xmonad
  fi

  echo 'Symlinking xmonad config...'

  ln -sf ~/.dotfiles/xmonad/xmonad.hs ~/.xmonad/xmonad.hs

  echo 'Symlinking stalonebar config...'

  ln -sf ~/.dotfiles/xmonad/stalonetrayrc ~/.stalonetrayrc

  echo 'Symlinking xmobar config...'

  ln -sf ~/.dotfiles/xmonad/xmobarrc ~/.xmobarrc
fi

echo "That's all for now!  You might want to 'rbenv rehash'"

